## Summary

<!-- What does this change do, and why? -->

## Related issue

<!-- Fixes #123 / Relates to #123, or "N/A" -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Documentation
- [ ] Refactor / chore (no user-facing change)
- [ ] CI / build

## Checklist

- [ ] `gofmt -l .` prints nothing
- [ ] `go vet ./...` passes
- [ ] `go test ./... -v -race` passes
- [ ] `golangci-lint run` passes
- [ ] No new external dependencies were added (or, if unavoidable, the reason is explained below — see "no external dependencies" in `CLAUDE.md`)
- [ ] Docs (`docs/architecture.md`, `docs/configuration.md`, `README.md`) updated if behavior or config changed

## Testing

<!-- How did you verify this? e.g. ran against a real Immich server, tested with a specific DLNA client/TV, added/updated unit tests. -->

## Notes for reviewers

<!-- Anything else worth calling out: tradeoffs, follow-up work, things you're unsure about. -->
